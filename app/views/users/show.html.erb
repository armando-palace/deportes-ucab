<h1 align="center">¡Bienvenido, <%= current_user.first_name || current_user.email %>!</h1>

<div class="row">
  <div class="col-sm-6">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><b>Datos Personales</b></h5>
        <p class="card-text"><b>CI:</b> <%= current_user.id_card || "No indicado" %></p>
        <p class="card-text">
          <div class="row">
            <div class="col">
              <b>Nombre:</b> <%= current_user.first_name || "No indicado" %>
            </div>
            <div class="col">
              <b>Apellido:</b> <%= current_user.last_name || "No indicado" %>
            </div>
          </div>
        </p>
        <p class="card-text"><b>Correo:</b> <%= current_user.email %></p>
        <p class="card-text">
          <b>Teléfono:</b> <%= if current_user.phone then current_user.phone.number else "No indicado" end %>
        </p>
        <%= link_to "Modificar", edit_user_registration_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
  <div class="col-sm-6">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><b>Datos Deportivos</b></h5>
        <p class="card-text">
          <b>Deportes que practicas:</b>
        </p>
        <p class="card-text">
          <b>¿Delegado?:</b>
          <%= current_user.has_role?("delegate") ? "Sí" : "No" %>
        </p>
      </div>
    </div>
  </div>
</div>

<br>

<div class="row justify-content-center">
  <div class="col-sm-6">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><b>Datos Universitarios</b></h5>
        <p class="card-text"><b>Escuela:</b> <%= current_user.school ? current_user.school.name :  "No has seleccionado una escuela aún" %></p>
        <p class="card-text"><b>Semestre / Año:</b></p>
        <%= link_to "Modificar", schools_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>