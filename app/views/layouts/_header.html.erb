<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">Deportes UCAB</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="deportesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Deportes</span>
        </a>
        <div class="dropdown-menu" aria-labelledby="deportesDropdown">
          <% Sport.all.each do |s| %>
            <%= link_to s.name, s, class: "dropdown-item" %>
          <% end %>
          <% if can? :manage, Sport %>
            <div class="dropdown-divider"></div>
            <%= link_to "Agregar", "#", class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <% if can? :manage, Player %>
        <li class="nav-item">
          <%= link_to "Jugadores", admin_players_path, class: "nav-link" %>
        </li>
      <% end %>
      <% if can? :manage, User %>
        <li class="nav-item">
          <%= link_to "Usuarios", admin_users_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item dropleft">
          <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="profileDropdown">
            <%= link_to "Mi Perfil", [:admin, current_user], class: "dropdown-item" %>
            <div class="dropdown-divider"></div>
            <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Iniciar Sesión", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "¡Regístrate!", new_user_registration_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
